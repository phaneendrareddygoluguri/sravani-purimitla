const SkillsIndex = () => {
  const React = document.React;
  const { About, CourseCard } = React.Imports(({ utils }) => utils);
  return (
    <>
      <About />
      <CourseCard />
    </>
  );
};
export default SkillsIndex;
