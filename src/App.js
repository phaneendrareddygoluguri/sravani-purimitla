function App() {
  const React = document.React;
  const { Routes } = React.Imports(({ metaComponents }) => metaComponents);
  return (
    <div>
      <Routes />
    </div>
  );
}

export default App;
